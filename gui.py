import tkinter as tkimport randomfrom PIL import ImageTk,Imagedataset = ["Diseases can make anyone sick regardless of their race or ethnicity.",           "the immediate risk of becoming seriously ill from COVID-19 is thought to be low.",           "Someone who has completed quarantine does not pose a risk of infection to other people."           , "Wash your hands often with soap and water for at least 20 seconds","in public, wear a cloth face covering that covers your mouth and nose." ,"Avoid touching your eyes, nose, and mouth with unwashed hands."]window = tk.Tk()window.title("COVID-19 Food Safety Measures")window.geometry('1000x250')my_img = ImageTk.PhotoImage(Image.open("L.jpg"))background_label = tk.Label(window, image = my_img)background_label.place(x=0, y=0, relwidth=1, relheight=1)frame = tk.LabelFrame(window, text = "Check Your Food",padx = 10, pady = 10)frame.pack(padx = 0, pady = 12.5)frame.configure(background = "orange")factLabel = tk.Label(frame)label2 = tk.Label(frame)def show():    check = e1.get()    print("Image: ", check)    e1 = tk.Entry(frame)e1.grid(column = 1, row = 1)btn1 = tk.Button(frame, text='Enter Image :', command=show)btn1.grid(column = 0 , row = 1)def commence():    global factLabel     global label2    factLabel.destroy()    label2.destroy()    response = tk.messagebox.askyesno(title = "Lets Check that food", message = "Food checking program will begin, ready to proceed?")    if response == True:        tk.messagebox.showinfo("Food Check " ,"Commencing Food Checking Sequence, Analyzing Images")        factLabel = tk.Label(frame)        factLabel.config(text = str(random.choice(dataset)))        factLabel.grid(column = 1, row = 0)        factLabel.configure(background = "green")    elif response == False:        tk.messagebox.showinfo("Food Check Halted","Process terminated")        label2 = tk.Label(frame, text = "Come back later!")        label2.grid(column = 1, row = 0)        label2.configure(background = "green")btn3 = tk.Button(frame, text="Start", bg = 'red' ,command = commence)btn3.grid(column = 0, row = 0)def close_window():    window.destroy()buttonEnd = tk.Button(window,text = "close application", command = close_window)buttonEnd.pack()window.mainloop()